<UserControl x:Class="WebBrowser.Views.TabStripView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:WebBrowser.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="44" d:DesignWidth="700">
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- A tab-shaped button: transparent, rounded, hover/active highlight via the IsActive flag on TabViewModel. -->
            <Style x:Key="TabSelectButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Focusable" Value="False" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="TabBackground"
                                    Background="Transparent"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter TargetName="TabBackground" Property="Background" Value="#338A8A8A" />
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsActive}" Value="False" />
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="TabBackground" Property="Background" Value="#228A8A8A" />
                                </MultiDataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal">
        <ItemsControl ItemsSource="{Binding Tabs}" Focusable="False">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:TabViewModel}">
                    <!-- Grid lets the close button overlay the right edge of the select button. -->
                    <Grid Margin="3,5" MinWidth="130" MaxWidth="220" Height="32">
                        <!-- Whole-tab select button -->
                        <Button Style="{StaticResource TabSelectButtonStyle}"
                                AutomationProperties.Name="{Binding Title}"
                                Command="{Binding DataContext.SelectTabCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <StackPanel Orientation="Horizontal" Margin="10,0,30,0">
                                <Image Width="16" Height="16"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="HighQuality"
                                       Source="{Binding Favicon}">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Favicon}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock Text="{Binding Title}"
                                           TextTrimming="CharacterEllipsis"
                                           VerticalAlignment="Center"
                                           FontSize="12" />
                            </StackPanel>
                        </Button>
                        <!-- Close button overlay (topmost at the right edge) -->
                        <ui:Button Appearance="Transparent"
                                   Icon="{ui:SymbolIcon Dismiss24}"
                                   AutomationProperties.Name="Close tab"
                                   Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   CommandParameter="{Binding}"
                                   Width="24" Height="24"
                                   Padding="0"
                                   HorizontalAlignment="Right" VerticalAlignment="Center"
                                   Margin="0,0,3,0"
                                   Focusable="False"
                                   ToolTip="Close tab" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ui:Button Appearance="Transparent"
                   Icon="{ui:SymbolIcon Add24}"
                   AutomationProperties.Name="New tab"
                   Command="{Binding NewTabCommand}"
                   Width="34" Height="34"
                   Padding="0"
                   Margin="3,5"
                   Focusable="False"
                   ToolTip="New tab (Ctrl+T)" />
    </StackPanel>
</UserControl>
